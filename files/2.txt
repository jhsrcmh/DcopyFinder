反抄袭检测算法的文本预处理部分涉及到文档匹配单位的分割，字符串结构的设计、中英文字符的处理方法等问题，其设计的好坏直接影响到算法的整体性，因此这一环节的重要性是不容忽视的。
本文提出的 文本分割方法为以字符快（300-400 个字符 ）作为信息提取单位，其实现过
程如下： 
1)设定 300 为字符读取的临界值，判断读取的字符个数，如果读取的字符个
数到达 300，则执行第 2 步；如果出现段的结束标志且字符读取个数小于 300，则


处理完成的字符串集合中有中文字符串和英语字符串，然而由于中英文字符编码格式的差异性，通常情况下中文字符占两个字节，英文字符占一个字节，这样的情况给字符匹配结构的建立增加了难度。本文首先采用字符串首字符构造哈希表，其构造方法如下例所示。后继字符构造为树形匹配结构，将字符的内码值映射为成叶子节点的形式，中文字符占两个节点英文字符占一个节点。在构造过程中先将字符串集合的首字符的内码值映射为哈希表，并以每个哈希表项作为树的根节点，然后将字符串的后继字符的内码值映射为叶子节点，具有相同根节点的子串同为根节点的子节点，相互之间为兄弟

反抄袭检测算法的文本预处理部分涉及到文档匹配单位的分割，字符串结构的设计、中英文字符的处理方法等问题，其设计的好坏直接影响到算法的整体性，因此这一环节的重要性是不容忽视的。
本文提出的 文本分割方法为以字符快（300-400 个字符 ）作为信息提取单位，其实现过
程如下： 
1)设定 300 为字符读取的临界值，判断读取的字符个数，如果读取的字符个
数到达 300，则执行第 2 步；如果出现段的结束标志且字符读取个数小于 300，则
跳转至第 3 步；否则继续读取直至符合这两个条件之一； 
2)如果读取字符到达临界值 300 时，判断当前读取的字符是否为该句的结尾
部分，如果是则读取停止继续下一步，否则继续读取直至将该句整句读入为止； 
3)将 读取出来的文本块以句子为边界再次 分割为字符串集合构造哈希匹配机
转入字符串匹配阶段； 
4)判断要读取的文档是否到达末尾，否则跳至第 1 步重复操作直至文档结束。

处理完成的字符串集合中有中文字符串和英语字符串，然而由于中英文字符编码格式的差异性，通常情况下中文字符占两个字节，英文字符占一个字节，这样的情况给字符匹配结构的建立增加了难度。本文首先采用字符串首字符构造哈希表，其构造方法如下例所示。后继字符构造为树形匹配结构，将字符的内码值映射为成叶子节点的形式，中文字符占两个节点英文字符占一个节点。在构造过程中先将字符串集合的首字符的内码值映射为哈希表，并以每个哈希表项作为树的根节点，然后将字符串的后继字符的内码值映射为叶子节点，具有相同根节点的子串同为根节点的子节点，相互之间为兄弟

